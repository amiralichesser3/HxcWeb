@using global::HxcUi.Services.Auth.Dto
<h3>Profile</h3>
@if (UserProfile != null)
{
    <MudText>
        @UserProfile.UserName
    </MudText>
}

@code
{
    [Parameter]
    public UserProfile? UserProfile { get; set; }
}